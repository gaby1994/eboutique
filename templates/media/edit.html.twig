{% extends 'base.html.twig' %}

{% block title %}Modifier le Média{% endblock %}

{% block body %}
    <div class="container d-flex flex-column align-items-center justify-content-center mt-5">
        <h1 class="mb-4">Modifier le Média</h1>

        <div class="w-50">
            {{ include('media/_form.html.twig', {'button_label': 'Modifier'}) }}
            <br>
        </div>
        <div class="text-center w-25">
            {{ include('media/_delete_form.html.twig') }}
        </div>
        <div class="text-center mt-3 w-25">
            <a href="{{ path('app_media_index') }}" class="btn btn-secondary mt-2">
                Retour à la liste des Médias
            </a>
        </div>
    </div>
{% endblock %}