{% extends 'base.html.twig' %}

{% block title %}Modifier la Catégorie{% endblock %}

{% block body %}
    <div class="container d-flex flex-column align-items-center justify-content-center mt-5">
        <h1 class="mb-4">Modifier la Catégorie</h1>

        <div class="w-50">
            {{ include('category/_form.html.twig', {'button_label': 'Modifier'}) }}
            <br>
        </div>
        <div class="text-left w-25">
            {{ include('category/_delete_form.html.twig') }}
        </div>
        <div class="mt-3 w-25">
            <a href="{{ path('app_category_index') }}" class="btn btn-secondary mt-2">
                Retour à la liste des Catégories
            </a>
        </div>
    </div>
{% endblock %}
