<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
    <div class="container">

        {# Logo centré sur mobile #}
        <a class="navbar-brand mx-auto d-lg-none" href="{{ path('app_home') }}">
            <img class="header-logo w-50" src="/img/logo.png" alt="Logo">
        </a>

        {# Bouton hamburger pour mobile #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false">
            <span class="navbar-toggler-icon"></span>
        </button>

        {# Contenu du menu #}
        <div class="collapse navbar-collapse" id="navbarContent">

            {# Desktop : Accueil + Catalogue à gauche #}
            <ul class="navbar-nav d-none d-lg-flex me-auto">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active fw-bold text-primary{% endif %}"
                       href="{{ path('app_home') }}">
                        <i class="fa-solid fa-house me-1"></i> Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_list_products' %}active fw-bold text-primary{% endif %}"
                       href="{{ path('app_list_products') }}">
                        Catalogue
                    </a>
                </li>
            </ul>

            {# Desktop : logo centré #}
            <a class="navbar-brand mx-auto d-none d-lg-block" href="{{ path('app_home') }}">
                <img class="header-logo w-75 h-75" src="/img/logo.png" alt="Logo">
            </a>

            {# Desktop : Connexion / Inscription (ou Profil/Admin) à droite #}
            <ul class="navbar-nav d-none d-lg-flex ms-auto">

                {# Lien Panier uniquement si pas admin #}
                {% if not is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_cart' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_cart') }}">
                            <i class="fa-solid fa-cart-shopping"></i> Panier
                        </a>
                    </li>
                {% endif %}

                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link text-danger fw-bold {% if app.request.attributes.get('_route') == 'app_administration' %}active{% endif %}"
                               href="{{ path('app_administration') }}">
                                <i class="fa-solid fa-gear me-1"></i> Administration
                            </a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.attributes.get('_route') == 'app_profile' %}active fw-bold text-primary{% endif %}"
                               href="{{ path('app_profile') }}">
                                <i class="fa-solid fa-user me-1"></i> {{ app.user.firstName }}
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_logout' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_logout') }}">
                            <i class="fa-solid fa-right-from-bracket me-1"></i> Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_login' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_login') }}">
                            <i class="fa-solid fa-right-to-bracket me-1"></i> Connexion
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_register' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_register') }}">
                            <i class="fa-solid fa-user-plus me-1"></i> Inscription
                        </a>
                    </li>
                {% endif %}
            </ul>

            {# Mobile : tout centré quand menu hamburger ouvert #}
            <ul class="navbar-nav d-lg-none w-100 d-flex flex-column justify-content-center align-items-center gap-2 mt-2">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active fw-bold text-primary{% endif %}"
                       href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_list_products' %}active fw-bold text-primary{% endif %}"
                       href="{{ path('app_list_products') }}">Catalogue</a>
                </li>

                {# Mobile : Admin / Profil #}
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link text-danger fw-bold {% if app.request.attributes.get('_route') == 'app_administration' %}active{% endif %}"
                               href="{{ path('app_administration') }}">
                                Administration
                            </a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.attributes.get('_route') == 'app_profile' %}active fw-bold text-primary{% endif %}"
                               href="{{ path('app_profile') }}">
                                {{ app.user.firstName }}
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_logout' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_login' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_register' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}

                {# Mobile : Panier uniquement si pas admin #}
                {% if not is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_cart' %}active fw-bold text-primary{% endif %}"
                           href="{{ path('app_cart') }}">Panier</a>
                    </li>
                {% endif %}
            </ul>

        </div>
    </div>
</nav>
