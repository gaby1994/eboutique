{% extends 'base.html.twig' %}

{% block title %}Modifier une Adresse de Livraison{% endblock %}

{% block body %}
    <div class="container d-flex flex-column align-items-center justify-content-center mt-5">
        <h1 class="mb-4">Modifier une Adresse de Livraison</h1>

        <div class="w-50">
            {{ include('customer_address/_form.html.twig', {'button_label': 'Modifier'}) }}
            <br>
        </div>
        <div class="text-left w-25">
            {{ include('customer_address/_delete_form.html.twig') }}
        </div>

        <div class="mt-3">
            <a href="{{ path('app_profile_addresses') }}" class="btn btn-secondary mt-2">
                Retour à la liste des adresses de livraison
            </a>
        </div>
    </div>
{% endblock %}
